{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/ModelSpecData",
  "definitions": {
    "ModelSpecData": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "file": {
          "type": "string",
          "description": "GLB path under `assets/`, e.g. `models/units/dwarf.glb`"
        },
        "yawDeg": {
          "type": "number",
          "description": "extra yaw in degrees on top of the forward convention (MODEL_FWD_YAW)"
        },
        "pitch": {
          "type": "number"
        },
        "roll": {
          "type": "number"
        },
        "offset": {
          "type": "object",
          "properties": {
            "x": {
              "type": "number"
            },
            "y": {
              "type": "number"
            },
            "z": {
              "type": "number"
            }
          },
          "additionalProperties": false
        },
        "scale": {
          "type": "number"
        },
        "stretch": {
          "type": "object",
          "properties": {
            "x": {
              "type": "number"
            },
            "y": {
              "type": "number"
            },
            "z": {
              "type": "number"
            }
          },
          "additionalProperties": false,
          "description": "Extra non-uniform scale after height normalize (local axes). Used to stretch bat / crow wings wider without growing body height."
        },
        "skinned": {
          "type": "boolean",
          "description": "Rigged GLB — keep SkinnedMesh; battle anim is unitAnimated ."
        },
        "bakePose": {
          "type": "object",
          "properties": {
            "clip": {
              "type": "string"
            },
            "time": {
              "type": "number"
            }
          },
          "additionalProperties": false,
          "description": "Pose a skinned clip then bake to static meshes for InstancedMesh. `clip`: substring of clip name, or `first` / `longest` / `shortest`. Default time `0` = first frame."
        },
        "animation": {
          "$ref": "#/definitions/ModelAnimation",
          "description": "Battle animation clips for a rigged model (needs `skinned`)."
        },
        "wingFlap": {
          "$ref": "#/definitions/WingFlapData",
          "description": "Wing-flap shader stroke for a winged model (crow rider, bat, …), defined per model."
        }
      },
      "required": [
        "file"
      ],
      "description": "A model as authored data — the shape a JSON content file will hold: a file name and degrees instead of a built URL and radians."
    },
    "ModelAnimation": {
      "type": "object",
      "properties": {
        "walk": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "range": {
              "$ref": "#/definitions/ClipTimeRange",
              "description": "Keep only this time window of the clip (seconds)."
            },
            "clip": {
              "$ref": "#/definitions/ClipPick",
              "description": "Which clip: a substring of its name (`\"walk\"`, `\"pitch_baseball\"`), `\"longest\"` / `\"shortest\"` by duration, or an index."
            },
            "speed": {
              "type": "number",
              "description": "Playback rate (default 1). Higher = less slide on walks, faster long swings."
            }
          },
          "required": [
            "clip"
          ],
          "description": "Locomotion loop (swagger / walk / run)."
        },
        "fire": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "range": {
              "$ref": "#/definitions/ClipTimeRange",
              "description": "Keep only this time window of the clip (seconds)."
            },
            "hold": {
              "type": "boolean",
              "description": "Keep the attack playing even if the mesh moves (crowd jostle / turn). Melee windups need this — by default the attack is dropped as soon as the unit relocates so long attack poses don't stick after the swing."
            },
            "clip": {
              "$ref": "#/definitions/ClipPick",
              "description": "Which clip: a substring of its name (`\"walk\"`, `\"pitch_baseball\"`), `\"longest\"` / `\"shortest\"` by duration, or an index."
            },
            "speed": {
              "type": "number",
              "description": "Playback rate (default 1). Higher = less slide on walks, faster long swings."
            }
          },
          "required": [
            "clip"
          ],
          "description": "One-shot attack; omit if the asset has none."
        },
        "death": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "fallLocal": {
              "type": "object",
              "properties": {
                "x": {
                  "type": "number"
                },
                "z": {
                  "type": "number"
                }
              },
              "required": [
                "x",
                "z"
              ],
              "additionalProperties": false,
              "description": "Where the clip lays the body in model XZ (rest forward −Z). Used to turn the body so the fall lines up with the killing blow (ogre `fall`: head travels toward −X, the character's left)."
            },
            "clip": {
              "$ref": "#/definitions/ClipPick",
              "description": "Which clip: a substring of its name (`\"walk\"`, `\"pitch_baseball\"`), `\"longest\"` / `\"shortest\"` by duration, or an index."
            },
            "speed": {
              "type": "number",
              "description": "Playback rate (default 1). Higher = less slide on walks, faster long swings."
            }
          },
          "required": [
            "clip"
          ],
          "description": "One-shot death / tip-over clip; omit to keep the procedural death tip."
        }
      },
      "required": [
        "walk"
      ],
      "additionalProperties": false,
      "description": "Battle animation of a rigged GLB (`\"animation\"` in `assets/data/models/<id>.jsonc`). A model with this block is driven by an AnimationMixer instead of an InstancedMesh; it needs `\"skinned\": true`."
    },
    "ClipTimeRange": {
      "type": "object",
      "properties": {
        "start": {
          "type": "number"
        },
        "end": {
          "type": "number"
        }
      },
      "required": [
        "start",
        "end"
      ],
      "additionalProperties": false,
      "description": "Seconds window inside an authored clip (inclusive start, exclusive-ish end via subclip)."
    },
    "ClipPick": {
      "anyOf": [
        {
          "type": "string",
          "const": "longest"
        },
        {
          "type": "string",
          "const": "shortest"
        },
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ],
      "description": "Pick a clip when the exporter names them uselessly (NlaTrack / NlaTrack.001). `longest` / `shortest` by duration; or a substring of the clip name; or index."
    },
    "WingFlapData": {
      "type": "object",
      "properties": {
        "axis": {
          "$ref": "#/definitions/WingFlapAxis",
          "description": "Wings along ±X flap around Z (default); wings along ±Z flap around X."
        },
        "flapAmp": {
          "type": "number",
          "description": "Stroke amplitude in radians at the tip (default 0.88)."
        },
        "flapSpeed": {
          "type": "number",
          "description": "Flap speed at full rate, radians of phase per second (default 10.5)."
        },
        "flapBias": {
          "type": "number",
          "description": "Negative centers the stroke below the model's rest pose (default -0.28)."
        },
        "tipFloor": {
          "type": "number",
          "description": "Shoulder motion as a fraction of tip motion (default 0.36)."
        },
        "tipPower": {
          "type": "number",
          "description": "Curve exponent: lower moves mid-wing more, higher is tip-only whip (default 1.32)."
        },
        "innerRFrac": {
          "type": "number",
          "description": "Hinge as a fraction of the wing's outer radius (default 0.22)."
        },
        "minYFrac": {
          "type": "number",
          "description": "Wing mask starts this fraction up from the model's floor (default 0.22)."
        },
        "maxYCutFrac": {
          "type": "number",
          "description": "Wing mask cuts this fraction off the model's top (default 0.04)."
        },
        "outerRScale": {
          "type": "number",
          "description": "Scale on the measured outer radius; >1 includes drooping tips (default 0.98)."
        },
        "softYFrac": {
          "type": "number",
          "description": "Soft fade outside the height band, as a fraction of model height (default 0.03)."
        }
      },
      "additionalProperties": false,
      "description": "A winged model's flap stroke (`\"wingFlap\"` in `assets/data/models/<id>.jsonc`). Defined per model — each bird type gets its own stroke. Wing radii and heights are measured from the mesh; these shape the motion on top."
    },
    "WingFlapAxis": {
      "type": "string",
      "enum": [
        "z",
        "x"
      ],
      "description": "Wings along ±X → flap with local Z rotation (crow). Wings along ±Z → X rotation (dragon, +X forward)."
    }
  },
  "allowComments": true,
  "allowTrailingCommas": true
}
