/**
 * Content helpers (unitName, cardTitle, term, …) land here as catalogs grow.
 * Import `t` from `./index` at call sites for now — keep this module free of
 * a circular import until helpers need shared formatting.
 */
export {};
